namespace Antymology.Simulation
{
    public static class SimulationSettings
    {
        public static bool FastMode = false;
        public static float TimeScaleMultiplier = 10f; // when fast mode is enabled, multiply delta time
    }
}
